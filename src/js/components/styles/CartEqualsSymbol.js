import styled from "styled-components";

const CartEqualsSimbol = styled.div`
	display: flex;
	flex-flow: column nowrap;
	justify-content: space-between;
	height: 30px;
`;

export default CartEqualsSimbol;
